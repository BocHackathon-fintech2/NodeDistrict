import { Component, ViewChild } from '@angular/core';
import { Router } from '@angular/router';

@Component({
    moduleId: module.id,
    selector: 'auth-app-route',
    templateUrl: './auth.component.html'
})


export class AuthComponent {

}